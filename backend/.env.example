PORT=5000
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.smmiw1z.mongodb.net/ConstructionDB?retryWrites=true&w=majority&appName=Cluster0
NODE_ENV=development
JWT_SECRET=cmms_secret_key_2024

# SMTP config for emailing Purchase Orders to suppliers (configured here for Google Gmail SMTP)
# Note: For Gmail, use an App Password (not your primary password) generated in your Google Account settings.
SMTP_SERVICE=gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=elsconstruction2026@gmail.com
SMTP_PASS=your_gmail_app_password
SMTP_FROM=ELS Construction <elsconstruction2026@gmail.com>

# Mailtrap sandbox fallback (commented out)
# SMTP_HOST=sandbox.smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=a922ac8935cee3
# SMTP_PASS=44f5eb9a5c280e
# SMTP_FROM=ELS CMMS <procurement@elsconstruction.com>

# Stripe payment gateway - replace with your own test keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# Get this by running: stripe listen --forward-to localhost:5000/api/payments/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret
STRIPE_CURRENCY=lkr
FRONTEND_URL=http://localhost:3000
